import java.util.*;

/**
 * I have to add two checks to this. One will check if  1/2 or more of the dice are 1. If true print ROLL FAILED
 * if false it prints individual rolls and displays sum.
 * 
 * 2nd check must cheks If any of the results are the same as the die type, that individual result is open ended, 
 * and another dice is rolled and added to the first result. This can happen multiple times.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class RollCheck
{
    // instance variables - replace the example below with your own
    private int x;

    /**
     * Constructor for objects of class RollCheck
     */
    public RollCheck()
    {
        // initialise instance variables
        x = 0;
    }

    /**
     * An example of a method - replace this comment with your own
     *
     * @param  y  a sample parameter for a method
     * @return    the sum of x and y
     */
    public int sampleMethod(int y)
    {
        // put your code here
        return x + y;
    }
}
